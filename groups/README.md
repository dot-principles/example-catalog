# groups

Group definitions for the `ptac` namespace. Each `.yaml` file defines a named group that maps to a list of principle IDs. Use `@ptac` in `.principles` to activate all PTAC principles at once.
