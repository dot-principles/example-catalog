# principles

Principle namespaces for this catalog. Each subdirectory is a namespace containing principle files, a `catalog.yaml`, and LLM context files (`.context-audit.md`, `.context-prime.md`, etc.).
